***定时中断***

 **EXTI外部中断**

![image-20260115153721579](C:\Users\Dzh19\AppData\Roaming\Typora\typora-user-images\image-20260115153721579.png)

![image-20260115160920833](C:\Users\Dzh19\AppData\Roaming\Typora\typora-user-images\image-20260115160920833.png)

- AFIO

> 有两个作用：复用功能重定义 和 中断引脚选择，在中断里的作用是 中断引脚选择

- EXTI

> 对引脚电平进行检测，触发中断，EXTI提交至NVIC

- NVIC

> NVIC相当于CPU的小助手，会对中断源优先级进行排序，最后将终端提交CPU

- 中断优先级

  > 优先级0~15，优先级数字越小，优先级越高。

  - 响应优先级

    > 当正在执行其他任务时，被中断打断，优先执行中断做出响应。这样的优先级叫做响应优先级

  - 抢占优先级

    > 当在执行其他中断时，抢占优先级优先级更高，可有暂停执行当前中断，优先执行抢占优先级高的中断。通常抢占优先级用于中断嵌套

- 硬件原理

> 边缘检测电路检测电平信号，不论是上升沿触发，下降沿触发，边沿触发，还是软件触发（手动触发），信号都要发到请求中断寄存器里，然后和中断屏蔽寄存器信号一起经过与门，输送到NVIC中断控制器。中断屏蔽寄存器可以控制中断，当它置0的时候，无论边沿检测电路里的信号是高电平还是低电平，中断都会被屏蔽。下面的事件屏蔽寄存器也是同理，当你产生中断事件不是为了触发中断而是为了中断打开其他外设的时候，就会走下面的通路。这个时候事件屏蔽寄存器就发挥着和中断屏蔽寄存器同样的作用。

- 初始化

  GPIO部分

  ```c
  RCC_APB2PeriphClockcmd(RCC_APB2Periph_GPIOA,ENABLE);
  //时钟使能
  GPIO_InitTypeDef GPIO_InitStructure;
  GPIO_InitStructure.GPIO_Mode=GPIO_Mode_Out_PP;
  GPIO_InitStructure.GPIO_Speed=50HZ;
  GPIO_InitStructure.GPIO_Pin=GPIO_Pin_0;
  GPIO_Init(GPIOA,&GPIO_InitStructure);
  //结构体初始化GPIO
  ```

  AFIO引脚选择

  ```c
  RCC_APB2PeriphClockcmd(RCC_APB2Periph_APIO,ENABLE);
  //AFIO使能
  GPIO_EXTILineConfig(GPIOPortSourceA,GPIOSource0);
  //将选择的引脚连接到EXTI总线上
  //这里选择对应的端口，和对应的引脚
  ```

  EXTI边沿检测与控制

  ```c
  EXTI_InitTypeDef EXTI_InitStructure;
  EXTI_InitStructure.EXTI_Line=EXTI_Line0；
  //中断接线
  EXTI_InitStructure.EXTI_Mode=EXTI_Mode_Interrupt;
  //中断模式选择 
  EXTI_InitStructure.EXTI_Trigger=EXTI_Trigger_Rasing_Downing;
  //配置触发模式
  EXTI_InitStructure.EXTI_Linecmd=ENABLE;
  //中断线使能
  EXTI_Init(&EXTI_InitStructure);
  ```

  NVIC配置

  ```c
  NVIC_InitTypedef NVIC_InitStructure;
  NVIC_InitStructure.NVIC_IRQChannel=EXTI_IRQn;
  //配置通道，EXTI_Line0对应EXTI_IRQn
  NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority=0;
  //配置抢占优先级
  NVIC_InitStructure.NVIC_IRQChannelSubPriority=0;
  //配置子优先级
  NVIC_InitStructure.NVIC_IRQChannelamd=ENABLE;
  //使能中断通道
  NVIC_Init(&NVIC_IRQChannel);
  ```

**定时中断（Timer定时器）**

![image-20260115181446131](C:\Users\Dzh19\AppData\Roaming\Typora\typora-user-images\image-20260115181446131.png)

![image-20260115181536932](C:\Users\Dzh19\AppData\Roaming\Typora\typora-user-images\image-20260115181536932.png)

- 时钟源和时钟模式

  - RCC内部时钟  内部时钟模式   RCC作为内部时钟源计数
  - ETR外部时钟   外部时钟模式2 ETR作为外部时钟源计数
  - ETR外部时钟  外部时钟模式1 ETR作为外部触发源，时钟来自内部，但ETR触发时，会触发TRGI，从而控制从模式控制器
  - ITRX其他定时器  外部时钟模式1 同样通过TRGI触发从模式控制器，主要用于定时器嵌套
  - Tlx捕获通道    编码器模式

- 时基单元

  - PSC 预分频器

  > 用于分频，当分频是1的时候相当于把原有频率分一半

  - CNT 计数器

  > 主要用来计数，最大计数值由PSC和ARR决定

  - ARR 自动重装器

  > 预设的申请中断的值

- NVIC

  > 对中断优先级进行排序，中断屏蔽，然后交给CPU

- 原理

  > CNT里进行计数，当CNT的值等于ARR时，会产生更新信号和更新事件，更新信号用来触发中断。更新事件是不触发中断，调用其他外设。可以根据实际情况选择