[TOC]

## 输入捕获的主从触发模式和PWMI模式测频率占空比

![](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-01-22 152815.png)

![屏幕截图 2026-01-22 152823](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-01-22 152823.png)

###  测量频率

----



- 测频法

  *测量一段时间里的频次，然后计算单位时间的频次*

- 测周法

  *测量单位频次所需要的时间*

  *==但是测量时间其实也是用另外一个时钟进行的，这个是时钟也有频率fc，以时钟的单位时间作为计量单位==*

> 当频次较高的时候，使用测频法。当频次较低的时候使用测周法。
>
> 中介频率是用来界定频率高低的频率点，将两种测量方式的频次代入就可以得到中介频率。
>
> 当频率大于中介频率的时候，用测频法。当频率 小于中介频率的时候，用测周法。



### 主从触发模式

----



![屏幕截图 2026-01-22 152843](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-01-22 152843.png)

![](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-01-15 181426.png)

-  **输入捕获 测频率**

1. **信号输入**

   信号通过==CH1==和==CH2== 输入（CH3和CH4不行）

2. **滤波和边沿检测**

   信号先是经过滤波器滤波，经过边沿触发器触发，选择输入方式

   IC1和IC2分别有来自==T1FP1和T2FP2==的信号，在输入的时候可以进行选择

   这样的好处是两个通道都有来自两个触发源的信号，当需要转变触发源的时候，更加方便

3. **锁存CCR**

   进入时基单元，按照输入捕获的流程。CNT一直计数，当有触发信号输入时，CNT的值立刻锁存进入CCR

4. **选择触发源触发从模式**

   ![](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-01-22 152837.png)

   - **主模式代表该模式下会产生信号，会通过TRGO向外输出。**

     **主模式有很多种，前面开启时钟的时候就用到了Enable,GPIO用到了Reset来设置当前状态，中断用到了Update产生更新事件**

     **OC1REF  OC2REF  OC3REF  OC4REF 这些在输出比较的时候用到**

     | 主模式     | 应用场景                                         |
     | ---------- | ------------------------------------------------ |
     | Reset      | 当事件产生的时候，定时器复位，用于定位器同步启动 |
     | Enable     | 用于使能定时器，启动计时                         |
     | Update     | 产生更新事件，作为TRGO，同步多个定时器           |
     | OC1/OC1REF | 输出比较 生成输出比较1事件，用于定时触发         |
     | OC2~OC4REF | 同上 只不过是由CH1换到了CH234                    |
     | TRGO       | 作为通用触发输出 可以连接到其他外设上            |

   - **触发源的选择则是影响触发方式**

     | 触发源  | 应用场景                                                     |
     | ------- | ------------------------------------------------------------ |
     | ITR0~3  | 内部触发，来自其他定时器的TRGO信号，用于定时器级联           |
     | TI1F_ED | TI1引脚的 双边触发，主要用于编码器计数，边沿计数             |
     | TI1FP1  | TI1引脚 滤波后的单边触发（CH1），主要用于PWM测频测占空比、输入捕获 |
     | TI2FP2  | TI2引脚 滤波后的单边触发（CH2），主要用于PWM测频测占空比、输入捕获 |
     | ETRF    | 外部触发，ETR引脚，用于外部时钟源或者外部事件触发            |

   - **从模式代表会接受来自主模式经过触发源的信号**

     | 从模式     | 应用场景                                                     |
     | ---------- | ------------------------------------------------------------ |
     | Closed     | 不从模式，定时器独立运行                                     |
     | Encoder1~3 | 编码器模式，用于读取旋转编码器的信号                         |
     | Reset      | 收到触发信号时复位计数器，常用于同步或重新启动计数。         |
     | Gated      | 门控模式，定时器在触发信号为高时运行，适合测量脉冲宽度或占空比。 |
     | Trigger    | 触发模式，收到触发信号后启动定时器，适用于事件驱动计时。     |
     | External   | 外部时钟模式1，使用外部信号作为时钟源，适合外部同步计数。    |

5. **从模式清零**

   选择T1FP1触发源，从模式选择Reset，可以实现从模式自动清零CNT，完全实现硬件自动化

6. **测量频率**

   首先我们通过控制极性，控制边沿触发。在每一次上升沿时基单元将CNT锁存进CCR，然后从模式自动清零，并继续计时。直到下一个上升沿继续执行上述操作。每次读取的CCR值就是每个周期的频次，所以就不断到的刷新CCR的值，得到新的频次。把N带入到计算频率的公式，就可以得到频率。

### PWMI模式同时测量频率和占空比

---



![](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-01-22 152851.png)

==刚才通过TI1FP1实现了对频率的测量，现在想要同时测量频率和占空比，这里就体现了TI1FP1和TI2FP2交叉输入的优势了==

==每一个通道既有来自TI1FP1的信号又有来自TI2FP2的信号，可以一个通道同时实现两个功能。==

- **输入捕获 测量占空比**

**通过控制极性，控制下降沿触发，在每次上升沿测频率时CNT清零，每次下降沿的时候，就将CNT锁存到CCR2，这样CCR2中就是高电平时间。占空比=CCR2/CCR1**