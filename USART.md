[TOC]

## USART

### 简介

![](USART.assets/屏幕截图 2026-02-11 162656.png)

![](USART.assets/屏幕截图 2026-02-11 162709-17745920886953.png)

![](USART.assets/屏幕截图 2026-02-11 162842.png)

![](USART.assets/屏幕截图 2026-02-11 162853.png)

### 电路组成

- 波特率发生器

  波特率是描述通信传输速度的一个量，代表单位时间传输的比特数

  波特率发生器通常是异步通信的关键，他约定了收发双方接收和发送的速率，所以波特率需要很精确

  波特率发生器的作用就像是TIM定时器的预分频器一样，将APB总线的频率进行分频达到和通信频率一致

- 接收移位寄存器

  接收来自RX引脚的数据帧，一位一位存进接收移位寄存器

- 接收数据寄存器（RDR）

  接收来自接收移位寄存器的数据，并申请中断让CPU读取或让DMA转运

- 发送数据寄存器（TDR）

  接收来自CPU或者DMA的数据，并自动发送到发送移位寄存器

- 发送移位寄存器

  接收到来自接收数据寄存器的数据，并通过TX引脚向外传输

- 数据帧

  - 起始位

    在起始位之前一直是高电平，而在起始位会变成低电平，当边沿检测电路检测到下降沿时，代表检测到起始位，会触发开始读取数据

  - 数据位

    在起始位之后就是数据位数据位的字长通常是8位或者9位，区别在于有无奇偶校验位

  - 停止位

    在数据位之后是停止位，停止位会置高电平，方便下次数据传输时检测下降沿

  - 奇偶校验位

    > 能够一定程度上检测出数据传输过程中的错误，但是无法检测出大规模翻转

    - 奇校验

      发送端会计算数据中1的个数，在校验位置0或1，使1的总数为奇数，接收端也会计算数据中1的个数是否为奇数与发送端比对

    - 偶校验

      发送端会计算数据中1的个数，在校验位置0或1，使1的总数为偶数，接收端也会计算数据中1的个数是否为偶数与发送端比对

- 空标志位（TXE）

  主要用于判断TDR中数据是否为空，当TXE为1时，CPU会中断写入数据，当TDR写满之后TXE会置0，CPU停止写入等待发送移位寄存器读取

- 非空标志位（RXNE）

  主要用于判断RDR数据是否非空，当RXNE为1时，接收移位寄存器会停止传输数据，CPU会中断读取数据，当RDR数据读取完之后RXNE会自动置0，发送移位寄存器继续写入

### 基本原理

- #### 发送原理

  1. 波特率发生器根据USART_BRR中设定的值，产生时钟信号，驱动发送移位寄存器
  2. CPU或DMA转运数据
  3. 当TXE为1时数据写入TDR，当TDR写满之后TXE置0，停止写入
  4. 当TXE为0并且发送移位寄存器为空时，数据写入发送移位寄存器
  5. 发送移位寄存器通过TX引脚向外传输
  6. 当TDR数据写入发送移位寄存器之后，TXE自动置1，继续向TRD下一次发送

- #### 接收原理

  1. 数据由RX引脚进入，波特率发生器会产生采样时钟
  2. 数据采样之后写入接收移位寄存器
  3. 接收移位寄存器将数据写入RDR中
  4. 当RDR数据写满之后RXNE置1，CPU会中断读取，同时接收移位寄存器停止向RDR传输数据
  5. 当RDR中数据读取完成之后，RXNE自动置0，接收移位寄存器继续下一次数据向DRD传输

### 采样抗噪

- 16倍过采样

  将一个比特周期均分16个，每个位置是一个采样点，对中间的三次进行采样（第789次），采取多数表决，当三个中有两个及以上相同的时候最终会采取多数。这样防止了偶发的噪声和毛刺。选择中间三次是因为，靠近两端的边沿更容易受收到噪音的干扰

- 起始位采样

  起始位采样仍然是过采样，但是要更加严格，因为起始位代表一个数据帧的开始，如果起始位检测错了，那么后面的所有数据位都会出现问题。所以起始位的三次采样会采取8910三次，停留半个比特周期时候进行采样，会大大降低噪声的概率。并且不再采取多数表决，而是必须三次都是0（起始位低电平）。

- 数据位采样

  数据位采样就是标准的16倍过采样，因为前面有起始位的严格把关，所以数据位就不需要8910位这么严谨了，对789进行采样就好，并且采用多数表决。并且在多数表决时，当出现三次采样不完全一致的时候，数据位会给其打上一个“可能是噪声”的标志，告诉CPU谨慎使用。这个设计是为了应对连续两个噪声造成信号跳变的情况.