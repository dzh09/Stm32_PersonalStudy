[TOC]

# I2C通信协议



## 简介

![](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-02-26 180243.png)

**在主机内部我们可以通过串口通信，但是如果我们想要和外部设备进行同步通信时，需要规定一个通信协议，保证数据的收发能够按照秩序进行**

## 硬件要求

![](C:\Users\Dzh19\Pictures\Screenshots\屏幕截图 2026-02-26 180301.png)

- **开漏输出+上拉电阻**
  - **SCL和SDA只能由每次只能由一个设备驱动，设置成其他模式，可能会有多个设备同时上拉或下拉，造成短路**
  - **开漏输出只有在需要输出0时下拉，需要输出1时，会被置为高阻态，然后由上拉电阻上拉**
  - **并且这种模式可以实现“线与”操作，即只有在所有的设备都输出1时，才会输出1。有助于实现多主机多从机**
- **GPIO（模拟I2C）**
  - **输出模式：开漏输出+上拉电阻**
  - **输入模式：浮空输入**
- **硬件I2C**
  - **采用复用开漏输出，硬件自动转变方向**


## **时序协议**

- **基本时序**

  | 时序状态 | SCL和SDA状态                                                 | 作用                                                         |
  | -------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
  | 空闲     | SCL高电平 SDA高电平                                          | 不进行读写的时候的状态                                       |
  | 起始     | SCL高电平期间SDA触发下降沿 随后SCL变为低电平                 | 告诉从机我要开始读/写了                                      |
  | 准备     | 只有在SCL低电平期间SDA才允许被更改 SCL低电平，改变SDA的值    | 发送方准备发送的值                                           |
  | 采样     | 在SCL为高电平期间会读取SDA数据                               | 接受方读取值                                                 |
  | 应答     | 在一个字节发送完毕之后 会进入第九周期 主机释放SDA  在上拉电阻作用下SDA置1 从机会将SDA下拉置0 代表应答 | 在发送完一个字节的数据之后，从机都要进行应答，告诉主机 我收到了 应答代表 收到/继续 |
  | 非应答   | 从机未将SDA下拉 SDA仍然是1 代表从机未应答                    | 从机没有收到或者主机要求不再读取 非应答代表 未收到/结束      |
  | 终止     | SCL变为高电平 SCL高电平期间SDA触发上升沿                     | 告诉从机读/写结束                                            |

- **写入操作**

  - 发出起始信号
  - 写入从机设备地址
  - 将读写位写0，表示进行写入操作
  - 从机应答
  - 写入从机寄存器地址
  - 从机应答
  - 写入数据
  - 从机应答
  - 发出终止信号

- **读取操作**

  - 发出起始信号
  - 写入从机设备地址
  - 将读写位写0，表示进行命令写入
  - 从机应答
  - 重新发出起始信号
  - 写入从机设备地址
  - 将读写位写1，表示进行读取操作
  - 从机应答
  - 主机读取数据
  - 主机发送非应答表示数据读完了
  - 主机发送终止信号

- **补充**

  - 由于SCL和SDA全权由主机控制，在每次主机发送完数据之后，进入应答之前，主机需要释放SDA，将SDA短暂的交给从机应答。释放SDA，SDA=1，并不是高电平，而是高阻态。当从机将SDA下拉时表示应答

  - 读取操作需要重新发送起始信号，目的是转变方向。（因为是主机全权控制，所以只有先进行写入操作但不写入数据，然后转向）

  - 进行读取操作时不需要写入寄存器地址，因为寄存器内部指针（类似栈，在每读取一个字节的数据之后，指针地址会自增，从而实现按顺序读取，主机只需要通过应答和非应答告诉从机还要不要读）

    